<%= form_with model: @book, class: 'book-form' do |f| %>

  <h3><%= form_title %></h3>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.select :author_id, Author.all.map{ |auth| [auth.name, auth.id] } %>

  <%= f.submit action_type, class: "book-form-submit-button" %>

<% end %>

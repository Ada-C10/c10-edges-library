This is in the app/views/books/new.html.erb file, and only affects this view!

<%= form_with model: @book, class: 'new-book-form' do |f| %>

  <h2>New Book Form</h2>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :author %>
  <%= f.text_field :author %>

  <%= f.submit "Create New Book", class: "new-book-submit-button" %>

<% end %>
